# 🚀 Інструкції по деплою застосунку

Ось кілька безкоштовних варіантів для запуску застосунку:

## 1. Railway.app (⭐ Рекомендовано - найпростіший)

**Переваги:**
- ✅ Безкоштовний план з $5 кредитами на місяць
- ✅ Простий деплой з GitHub
- ✅ Автоматичний HTTPS
- ✅ Не засинає (працює постійно)

**Як задеплоїти:**

1. Створіть репозиторій на GitHub і завантажте код
2. Зайдіть на [railway.app](https://railway.app) і зареєструйтесь через GitHub
3. Натисніть "New Project" → "Deploy from GitHub repo"
4. Виберіть ваш репозиторій
5. Railway автоматично визначить Node.js проект
6. Додайте змінні оточення (Environment Variables):
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `TELEGRAM_CHAT_ID` = ваш Chat ID
7. Готово! Застосунок запуститься автоматично

---

## 2. Render.com

**Переваги:**
- ✅ Безкоштовний план
- ⚠️ Може "засинати" після 15 хвилин неактивності (але автоматично прокидається)

**Як задеплоїти:**

1. Створіть репозиторій на GitHub
2. Зайдіть на [render.com](https://render.com) і зареєструйтесь
3. Натисніть "New" → "Web Service"
4. Підключіть GitHub репозиторій
5. Налаштування:
   - **Name**: dou-vacancy-tracker
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
6. Додайте Environment Variables:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
7. Натисніть "Create Web Service"

---

## 3. Fly.io

**Переваги:**
- ✅ Безкоштовний план (3 shared-cpu VMs)
- ✅ Не засинає
- ✅ Швидкий

**Як задеплоїти:**

1. Встановіть Fly CLI:
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. Створіть `fly.toml` (вже створено в проекті)
3. Зареєструйтесь:
   ```bash
   fly auth signup
   ```
4. Задеплойте:
   ```bash
   fly launch
   ```
5. Додайте змінні оточення:
   ```bash
   fly secrets set TELEGRAM_BOT_TOKEN=ваш_токен
   fly secrets set TELEGRAM_CHAT_ID=ваш_chat_id
   ```

---

## 4. GitHub Actions (Альтернатива - періодичний запуск)

Якщо не хочете постійно працюючий сервер, можна використати GitHub Actions для запуску кожні 30 хвилин.

**Переваги:**
- ✅ Повністю безкоштовно
- ✅ Не потребує окремого сервера
- ⚠️ Запускається тільки за розкладом (не постійно)

**Як налаштувати:**

1. Створіть файл `.github/workflows/check-vacancies.yml` (вже створено)
2. Додайте секрети в GitHub:
   - Settings → Secrets and variables → Actions
   - Додайте `TELEGRAM_BOT_TOKEN` та `TELEGRAM_CHAT_ID`
3. Запустіть workflow вручну або він запуститься автоматично за розкладом

---

## 5. Replit

**Переваги:**
- ✅ Безкоштовний план
- ✅ Простий інтерфейс
- ⚠️ Може засинати після неактивності

**Як задеплоїти:**

1. Зайдіть на [replit.com](https://replit.com)
2. Створіть новий Repl → "Import from GitHub"
3. Вставте URL вашого репозиторію
4. Додайте Secrets (🔒 іконка зліва):
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
5. Натисніть "Run"

---

## 📋 Порівняння

| Платформа | Безкоштовно | Постійна робота | Складність |
|-----------|-------------|-----------------|------------|
| Railway.app | ✅ ($5/міс) | ✅ | ⭐ Легко |
| Render.com | ✅ | ⚠️ Може засинати | ⭐ Легко |
| Fly.io | ✅ | ✅ | ⭐⭐ Середньо |
| GitHub Actions | ✅ | ⚠️ За розкладом | ⭐⭐ Середньо |
| Replit | ✅ | ⚠️ Може засинати | ⭐ Легко |

---

## 🎯 Рекомендація

**Для початку:** Railway.app або Render.com - найпростіші у використанні.

**Якщо потрібна гарантована робота 24/7:** Fly.io або Railway.app.

**Якщо хочете економити ресурси:** GitHub Actions (запускається тільки за розкладом).

---

## ⚙️ Важливо перед деплоєм

1. **Не комітьте `.env` файл!** Він вже в `.gitignore`
2. Додайте змінні оточення на платформі деплою
3. Переконайтеся, що `package.json` має правильний `start` скрипт
4. Для GitHub Actions - додайте секрети в репозиторій

---

## 🔧 Налаштування після деплою

Після деплою перевірте логи, щоб переконатися, що:
- ✅ Застосунок запустився
- ✅ Telegram токен працює
- ✅ Парсинг DOU працює
- ✅ Сповіщення надсилаються

Якщо щось не працює - перевірте логи на платформі деплою.

